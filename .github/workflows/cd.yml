name: Deploy Fake
on:
  workflow_call:
jobs:
    deploy-kubernetes:
       runs-on: ubuntu-latest
       steps:
            - name: Deploy no ambiente
              run: echo "Executando o deploy no ambiente de ${{ secrets.AMBIENTE_ENTREGA }}"
            - name: Curl Deploy no ambiente
              un: "curl -X POST https://webhook.site/5a51c018-d468-43f7-83c3-a1fd832ac068 -d '{ ambiente: ${{ secrets.AMBIENTE_ENTREGA }}}'"